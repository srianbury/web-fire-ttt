const LOCAL_STORAGE_USERNAME = "username";

const ROUTE_HOME = "/";
const ROUTE_JOIN = "/join";
const ROUTE_MATCH = "/match/:matchId";

const GAME_STATE_LOBBY = "lobby";
const GAME_STATE_PLAYING = "playing";
const GAME_STATE_GAMEOVER = "gameover";

export {
  LOCAL_STORAGE_USERNAME,
  ROUTE_HOME,
  ROUTE_JOIN,
  ROUTE_MATCH,
  GAME_STATE_LOBBY,
  GAME_STATE_PLAYING,
  GAME_STATE_GAMEOVER
};
